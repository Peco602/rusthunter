name: Validation

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2

    - name: Execution permissions
      run: chmod +x ./rusthunter.sh
      
    - name: Validation tests
      run: sudo ./rusthunter.sh --validation